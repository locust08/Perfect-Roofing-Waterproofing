{
  "name": "perfect-roofing-waterproofing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "postbuild": "node scripts/assert-redirect-destinations.mjs",
    "test": "vitest run",
    "preview": "astro preview --host 127.0.0.1",
    "preview:cloudflare": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.4"
  },
  "devDependencies": {
    "vitest": "^5.0.1",
    "wrangler": "^4.137.0"
  }
}
